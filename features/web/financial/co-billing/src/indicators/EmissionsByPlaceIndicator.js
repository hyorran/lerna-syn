import React from 'react'
import Indicator from '@syntesis/c-indicator/src/components/Indicator'

const EmissionsByPlaceIndicator = props => (
  <Indicator
    slug="cobilling_issues_by_cobilling_place_occurrences"
    { ...props }
  />
)

export default EmissionsByPlaceIndicator
